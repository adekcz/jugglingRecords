""" django configuration"""
from django.apps import AppConfig

class RecordsConfig(AppConfig):
    """Contains configuration for current application"""
    name = 'records'
    verbouse_name = 'Juggling Records'
